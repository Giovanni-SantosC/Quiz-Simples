# Quiz-Simples
Quiz interativo desenvolvido com JavaScript. Gerencia o fluxo de perguntas via arrays de objetos, renderiza opções dinamicamente no HTML e fornece feedback visual imediato (verde/vermelho) com delay automático entre as rodadas.
